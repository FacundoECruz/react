const validateNames = (players, newPlayer) => {
  console.log(players);

  // if (players.length !== 2) {
  //   players.map((p) => {
  //     if (p.name === newPlayer) {
  //       return false;
  //     }
  //   });
  // }
  return true;
};

export default validateNames;
